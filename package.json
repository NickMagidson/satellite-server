{
  "name": "satellite-server-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm --workspace apps/api run build",
    "dev": "npm --workspace apps/api run dev",
    "typecheck": "npm --workspace apps/api run typecheck"
  }
}
